<VirtualHost *:443>
    ServerName mini-kbp.site
    ServerAlias www.mini-kbp.site lk.mini-kbp.site panel.mini-kbp.site api.mini-kbp.site localhost
    ServerAdmin webmaster@localhost
    DocumentRoot /var/www/app/public

    SSLEngine on
    SSLCertificateFile /etc/ssl/mini-kbp/fullchain.pem
    SSLCertificateKeyFile /etc/ssl/mini-kbp/privkey.pem

    Alias /.well-known/acme-challenge/ /var/www/certbot/.well-known/acme-challenge/
    <Directory /var/www/certbot/.well-known/acme-challenge/>
        Options None
        AllowOverride None
        Require all granted
    </Directory>

    <Directory /var/www/app/public>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    <LocationMatch "^/(robots\.txt|sitemap\.xml|llms\.txt)$">
        Header set Cache-Control "public, max-age=3600, must-revalidate"
    </LocationMatch>
    <LocationMatch "^/og-default\.png$">
        Header set Cache-Control "public, max-age=86400"
    </LocationMatch>
    <LocationMatch "^/api/">
        Header set X-Robots-Tag "noindex, nofollow"
        Header set Cache-Control "no-store"
    </LocationMatch>

    Header always set Strict-Transport-Security "max-age=31536000; includeSubDomains" env=HTTPS

    ErrorLog ${APACHE_LOG_DIR}/ssl_error.log
    CustomLog ${APACHE_LOG_DIR}/ssl_access.log combined
</VirtualHost>
